An in-memory, NoSQL key-value store used as a high-performance database, cache, and message broker. Its primary characteristic is that it stores data in the server's main memory (RAM) 
rather than on a disk, which enables extremely fast read and write operations with sub-millisecond latency
